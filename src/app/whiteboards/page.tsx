import { type Metadata } from "next";
import WhiteboardsClient from "./WhiteboardsClient";

export const metadata: Metadata = {
  title: "Whiteboards | AI Flow Studio",
};

export default WhiteboardsClient;
